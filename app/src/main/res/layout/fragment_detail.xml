<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <androidx.core.widget.NestedScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:backgroundTint="#efefef"
        android:background="@android:color/white"
        android:padding="26dp">


        <androidx.constraintlayout.widget.ConstraintLayout
            android:background="#fff"
            android:elevation="26dp"
            android:padding="26dp"
            android:layout_width="match_parent"
            android:layout_height="wrap_content">

            <!-- Dog Image -->
            <ImageView
                android:id="@+id/dogDetailImageView"
                android:layout_width="0dp"
                android:layout_height="250dp"
                android:layout_marginBottom="16dp"
                android:contentDescription="@string/dog_image"
                android:scaleType="centerCrop"
                app:layout_constraintBottom_toTopOf="@id/dogBreedTextView"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toTopOf="parent"
                tools:srcCompat="@drawable/dog" />


            <!-- Dog Breed -->
            <TextView
                android:id="@+id/dogBreedTextView"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_marginBottom="8dp"
                android:textColor="@android:color/black"
                android:textSize="24sp"
                android:textStyle="bold"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toBottomOf="@id/dogDetailImageView"
                tools:text="Golden Retriever" />

            <!-- Dog Lifespan -->
            <TextView
                android:id="@+id/dogLifespanTextView"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_marginBottom="16dp"
                android:textColor="@android:color/darker_gray"
                android:textSize="16sp"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toBottomOf="@id/dogBreedTextView"
                tools:text="12-14 years" />


        </androidx.constraintlayout.widget.ConstraintLayout>
    </androidx.core.widget.NestedScrollView>
</layout>
